import 'package:flutter/material.dart';

class ThemeColors {
  static const Color primaryColor = Colors.blue; // Primary color for the app
  static const Color secondaryColor = Colors.green; // Secondary color, could be used for buttons or highlights

  // You can define more colors as needed
  static const Color backgroundColor = Colors.white;
  static const Color textColor = Colors.black;
}
